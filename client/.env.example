# Set this to your deployed Render backend URL in production.
# You can find it in the Render dashboard after the first deploy.
# Example: VITE_API_BASE_URL=https://krushak-server.onrender.com/api
# Leave blank for local dev (auto-uses http://localhost:5000/api)
VITE_API_BASE_URL=
